import { css } from '@emotion/css/macro';

export default css`
  position: relative;
  width: 100%;
  height: 100%;
  background-color: var(--white);
  box-shadow: 0 0 31px -11px rgba(0, 0, 0, 0.19);
  > .test {
    margin: 0 20px;
    padding: 10px;
  }
`;
